/**
 * Width and height (px) of the icon container above the value row.
 * Must be large enough for a 24–32 px icon with comfortable padding.
 */
export const STAT_CARD_ICON_BOX_SIZE = 48;

/**
 * Minimum width (px) of the label+value column inside the bottom row.
 * Prevents the labels from collapsing too aggressively when a sparkline is present.
 */
export const STAT_CARD_LABELS_MIN_WIDTH = 112;
