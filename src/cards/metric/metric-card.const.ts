// ----------------------------------------------------------------------

/**
 * Width and height (px) of the icon Box in the top-right corner of `MetricCard`.
 * The icon passed via the `icon` prop should match this size.
 */
export const METRIC_CARD_ICON_BOX_SIZE = 36;

/**
 * Width and height (px) of the `MetricCardDecoration` shape.
 * Can be overridden per-instance via `sx`.
 */
export const METRIC_CARD_DECORATION_SIZE = 140;
