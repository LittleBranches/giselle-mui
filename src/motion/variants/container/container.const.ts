/** Stagger delay between child animations (seconds). */
export const CONTAINER_STAGGER_CHILDREN = 0.05;

/** Delay before the first child animation starts (seconds). */
export const CONTAINER_DELAY_CHILDREN = 0.05;

/**
 * Stagger direction on exit — reverse order (`-1`).
 * Children that entered last will exit first.
 */
export const CONTAINER_EXIT_STAGGER_DIRECTION = -1;
