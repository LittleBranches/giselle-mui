/**
 * Initial rotation for flip-in animations (degrees).
 * Starts at -180° (behind the element) and rotates to 0°.
 */
export const FLIP_IN_ROTATION = -180;

/**
 * Final rotation for flip-out animations (degrees).
 * Rotates to 70° (tilting away from the viewer).
 */
export const FLIP_OUT_ROTATION = 70;
