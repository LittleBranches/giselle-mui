/** Maximum width (px) of the accordion list container. */
export const FAQ_CONTENT_MAX_WIDTH = 720;

/** Left offset (px) for the decorative vertical float line and stacked triangles. */
export const FAQ_FLOAT_LINE_LEFT = 80;

/** Left offset (px) for the float plus icons in the contact section divider. */
export const FAQ_PLUS_ICON_LEFT = 72;

/** Font size for accordion question headings — matches `h6` by default. */
export const FAQ_QUESTION_FONT_SIZE = '1.125rem';
