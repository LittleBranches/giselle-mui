import type { SxProps, Theme } from '@mui/material/styles';

// ----------------------------------------------------------------------

/** Root Box for `IconActionBar` — horizontal flex row, full width. */
export const iconActionBarRootSx: SxProps<Theme> = {
  gap: 1,
  width: 1,
  flexGrow: 1,
  display: 'flex',
};
