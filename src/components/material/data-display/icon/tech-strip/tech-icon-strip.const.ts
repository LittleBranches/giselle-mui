// ----------------------------------------------------------------------

/**
 * Width and height applied to `<svg>` children inside each icon slot.
 * Matches the minimum standalone decorative icon size (32 px).
 * Never set below 32 — see copilot-instructions minimum readable sizes table.
 */
export const TECH_ICON_STRIP_ICON_SIZE = 32;

/**
 * Font size for the label rendered below each icon.
 * Matches the minimum badge/pill label size (0.75 rem).
 * Never set below 0.75 rem.
 */
export const TECH_ICON_STRIP_LABEL_FONT_SIZE = '0.75rem';
